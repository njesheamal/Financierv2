%h1= @account.name
%p= @account.description
%p= @account.account_number
%p= @account.credit_limit

= link_to "Edit", edit_account_path(@account), class: "button"
= link_to "Delete", account_path(@account), method: :delete, data: { confirm: "Are you sure?" }, class: "button"
= link_to "Create Transaction", new_account_transaction_path(@account), class: "button"
= link_to "View Transactions", account_transactions_path(@account), class: "button"
%br/
%br/
%br/
%button= link_to "Spending Overview", spending_path(@account)