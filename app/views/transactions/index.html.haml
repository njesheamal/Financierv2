%h1 This is your transaction!
%ul
    - @transactions.each do |transaction|
        %li= transaction.created_at
        %li= transaction.amount
        %li= transaction.description
        = link_to "Make a Payment", new_account_payment_path(@account), class: "button"